{
  "name": "livesky-weather",
  "version": "2.0.0",
  "description": "LiveSky Weather Pro — premium weather web app",
  "private": true,
  "scripts": {
    "test": "node tests/smoke.js",
    "serve": "python3 -m http.server 8000 --bind 0.0.0.0 --directory docs"
  },
  "devDependencies": {
    "jsdom": "^24.1.0"
  }
}
