# Диагностика размера репозитория LiveSky (42 МБ)

**Дата:** 2026-08-29 · **Статус:** диагностика завершена → **чистка выполнена 2026-08-30** (по решению пользователя):
`git filter-repo` по 7 мусорным путям, переписаны 129 коммитов, force-push всех 24 веток и тега.
**Результат: история 43 МБ → 5,3 МБ.** Бэкап до чистки: `/tmp/livesky-backup.git`.
Ниже — исходный отчёт-диагностика (актуальна картина «до»).

## Вывод

GitHub сообщает размер **42,6 МБ**. Это НЕ рабочее дерево и НЕ node_modules —
это **история Git**. В текущем дереве файлы уже удалены, но их содержимое
осталось в старых коммитах, а GitHub считает все объекты репозитория.

## Что где лежит

| Часть | Размер | Комментарий |
|---|---|---|
| Рабочее дерево (tracked) | **8,4 МБ** | docs 8,1 МБ + android 1,3 МБ — нормально |
| `node_modules/` | 43 МБ | в Git **не входит** (уже в `.gitignore`), это локальная установка для тестов |
| `.git` (после полного fetch) | 44 МБ | = то, что GitHub и считает «размером репозитория» |
| История | 110 коммитов, 479+ объектов | единственный источник 42 МБ |

## Виновники — файлы, залитые через GitHub «Add files via upload» и позже удалённые из дерева, но оставшиеся в истории `main`

| Файл | Размер | Добавлен в |
|---|---|---|
| `Picsart_26-08-28_03-31-13-328.png` | 12,0 МБ | fc7006f |
| `Picsart_26-08-28_03-38-39-815.png` | 10,9 МБ | fc7006f |
| `LiveSky-debug.apk` | 5,9 МБ | b4a7382 |
| `Picsart_26-08-28_05-19-51-199.jpg` | 5,0 МБ | 774f31c |
| `Picsart_26-08-28_05-22-54-911.jpg` | 4,4 МБ | 774f31c |
| `app_icon.ico` | 0,3 МБ | 4945dea |
| `пример-эффекта-капель-на-стекле.png` | 0,2 МБ | 91eeaeb |
| **Итого мусора** | **≈38,7 МБ** | |

Остальные ~4–5 МБ — оправданный контент: vendored MapLibre (0,8 МБ), шрифты
Phosphor (~2,9 МБ), иконки, сам код. Файлы апк/keystore в `.gitignore` уже есть.

## Что можно сделать (по убыванию агрессивности)

1. **Полная очистка истории** — `git filter-repo --path PICSTART.. --invert-paths`
   (или BFG) по 7 путям выше + force-push.
   - Эффект: **−38,7 МБ**, репозиторий станет ~4–5 МБ.
   - Последствия: перепишутся хэши всех коммитов после этих файлов; всем, кто
     клонировал, нужен re-clone (`git pull` сломается); тег
     `v2.0.0-android-beta.1` придётся пересоздать; кэши GitHub Actions сбросятся.
   - Требует твоего явного согласия (переписывание истории запрещено по ТЗ).

2. **Ничего не делать** — 42 МБ влияет только на скорость клонирования;
   на работу сайта, APK-сборку и CI никак не влияет.

3. **Безопасная мелочь (опционально, без истории):** убрать устаревшие `.woff`
   дубликаты шрифтов и лишние Picsart-подобные файлы, если их кто-то держит
   в дереве — сейчас в дереве их нет, так что экономии фактически ноль.

## Рекомендация

Раз основная проблема — история, и чистка требует переписывания, я ничего не
удалял. Предлагаю: если репозиторий публичный и 42 МБ мешает (медленные
клоны) — сделаем фильтр-очистку с твоего согласия. Во всех остальных случаях
достаточно оставить как есть: контент репозитория (8,4 МБ) уже минимален.
